const exampleSchema = require('./example-schema')
const eventSchema = require('./event-schema')
const configurationItemSchema = require('./configuration-item-schema')
const incidentSchema = require('./incident-schema')
const solutionSchema = require('./solution-schema')
const temperatureHumiditySchema = require('./temperature-humidity-schema')

module.exports = {
  exampleSchema,
  eventSchema,
  configurationItemSchema,
  incidentSchema,
  solutionSchema,
  temperatureHumiditySchema
}
